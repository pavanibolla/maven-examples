pipeline {
    agent {
        label "master"
    }
    tools {
        maven 'maven'
    }
    stages {
       stage('envsetup') {
           steps {
              echo " mvn path setup "
           }
       }    
       stage('Build') {
          steps {
              echo "building the code"
              sh "cd helloworld"
              sh 'mvn compile'
          }
       }
       stage('Test') {
          steps {
            echo "mvn test"
            
          }
       }
       stage('Deploy') {
          steps {
            echo "mvn install"
          }
       }
    }
}
